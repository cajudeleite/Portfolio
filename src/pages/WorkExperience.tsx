import React from "react";
import { title } from "../utils/classNameLibrary";

const WorkExperience = () => {
  return <h1 className={`text-primary ${title}`}>Work Experience</h1>;
};

export default WorkExperience;
